/**
 * @format
 */

import { AppRegistry } from 'react-native';
import PuppybneApp from './PuppybneApp';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => PuppybneApp);
